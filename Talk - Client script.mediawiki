<p>This page has issues: if Bryan's comments "The virtual machine that runs client scripts is not included with the ship build of the game, so this resource is likely to be of no practical use." are confirmed then there is no point in including an example (in fact everything after that comment is superfluous).  However even if Bryan's comment are denied then the example should be revised as it includes at least one function that is not available to us (i.e. PrintToScreen).  --<a href="https://github.com/adarec1994/DragonAgeToolset/wiki/User%20-%20Sunjammer" title="User:Sunjammer">Sunjammer</a> 01:19, 29 November 2009 (UTC)
</p>
